# Package-Native Dashboard Migration Backlog

The executable migration candidate plan is generated by:

```bash
npm run dashboard:migration-codemod:plan
```

Generated output:

- `docs/design/dashboard-migration-codemod-plan.json`

Current migration priority:

1. Khashi VC ROC: move from static adapter bridge to package-native Tier 3C.
2. Media Business Operations: replace static dashboard renderer and sidebar gaps.
3. Business Mapper: promote from adapter shell to shared component dashboard.
4. Meal Assistant: replace server-rendered legacy dashboard shell.
5. Hermes OS and TLC Capital Group OS: move planned dashboards into package-native operating cockpits.

Do not clear codemod candidates by editing generated JSON. Clear them by
upgrading the downstream surface, regenerating the adoption report, and rerunning
the codemod plan.
